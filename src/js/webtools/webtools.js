
let ContractManager = require( './contractmanager.js' )
let RelayManager = require( './relaymanager.js' )
// let AddPastEvents = require( '../relayclient/addPastEvents.js')

// eslint-disable-next-line no-undef
webtools = {
	ContractManager,
	RelayManager,
	// AddPastEvents,
}
